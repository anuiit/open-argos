# Review long-context case

The input is long enough that the interesting evidence appears late in the
thread.

Important anchors:

- the last user correction supersedes the first summary;
- the patch target is still `bench_argos_quality.py`;
- the benchmark must not flatten the multi-turn trail into a single reply.

The review should show it kept the late correction in scope.

