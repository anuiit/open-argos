# Case spec

- The runner should accept `--dir` and keep file ordering deterministic.
- The benchmark should not collapse directory inputs into a single synthetic
  file.
- The reply should reference specific files and a small test change.
