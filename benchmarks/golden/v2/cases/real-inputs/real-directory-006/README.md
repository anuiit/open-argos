# Real directory benchmark case 006

This directory exists to verify that the benchmark runner can attach an entire
folder, not just a single file.

The model should inspect the companion files, identify the benchmark bug, and
keep the patch narrow.
