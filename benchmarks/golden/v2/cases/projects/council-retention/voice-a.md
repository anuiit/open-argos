# Voice A — reliability

Use isolated one-shot calls for the default workflow. They are easier to
replay and a provider timeout cannot poison persistent conversation state.
Sessions should remain opt-in until state repair has more production evidence.
