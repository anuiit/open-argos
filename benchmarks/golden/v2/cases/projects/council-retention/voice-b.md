# Voice B — continuity

Use persistent sessions for long design work. Re-sending the entire context is
expensive and loses correction history. The default can be safe if every turn
has an explicit outcome and stale turns are repaired visibly.
