The supplied evidence is incomplete, so it cannot support a confident winner.
It shows that offline execution exists and that provider behavior varies, but
contains no matched launch comparison.

Before deciding, verify unstable behavior against current official
documentation and collect repeated measurements with matched prompts, models,
budgets, timeouts, and machines. Report provider failures separately from
successful-output latency.

The remaining uncertainty is material: continuity quality, failure rate, and
cost are not measured on the same cases. Run a bounded experiment with at least
three repetitions per comparable cohort, publish the raw rows, and only then
state a confidence-qualified recommendation.
