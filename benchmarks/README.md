# argos-dev benchmark

Versioned local benchmark fixtures and frozen judge configuration for argos-dev (internal suite `argos-internal-quality`). Generated for Benchmark v1.0.0. Results under `benchmarks/results/` are intentionally gitignored.
