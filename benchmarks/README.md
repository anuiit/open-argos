# mosaic-dev benchmark

Versioned local benchmark fixtures and frozen judge configuration for mosaic-dev. Generated for Benchmark v1.0.0. Results under `benchmarks/results/` are intentionally gitignored.
